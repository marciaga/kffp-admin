const initialState = {
    playlists: [],
    currentPlaylist: {}
};

export default function playlistReducer (state = initialState, action) {
    switch (action.type) {

    default:
        return state;
    }
}
