import Boom from 'boom';

const sendResetPassword = async (request, reply) => {
    
};

const handlePasswordReset = async (request, reply) => {

};

export { sendResetPassword, handlePasswordReset };
