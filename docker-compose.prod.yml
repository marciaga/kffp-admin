version: "2"
services:
  web:
    build: .
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    links:
      - db
    command: yarn run build && yarn run start
    environment:
      NODE_ENV: production
  db:
    image: mongo
    ports:
      - "27017:27017"
